{
  "name": "signalhub",
  "private": true,
  "version": "1.0.0",
  "description": "SignalHub - AI-Powered Learning and Course Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx cross-env NEXT_DISABLE_SWC_LOCKFILE_PATCH=1 npm run dev:web",
    "web": "npx cross-env NEXT_DISABLE_SWC_LOCKFILE_PATCH=1 npm run dev:web",
    "dev:web": "npx cross-env NEXT_DISABLE_SWC_LOCKFILE_PATCH=1 npm run dev --workspace=@signalhub/web",
    "dev:mobile": "npm run start --workspace=@signalhub/mobile",
    "build:web": "npx cross-env NEXT_DISABLE_SWC_LOCKFILE_PATCH=1 npm run build --workspace=@signalhub/web",
    "build:packages": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:migrate": "supabase db reset"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "optionalDependencies": {
    "@next/swc-win32-x64-msvc": "^14.2.33",
    "@next/swc-linux-x64-gnu": "^14.2.33",
    "@next/swc-wasm-nodejs": "^14.2.33"
  },
  "overrides": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}


